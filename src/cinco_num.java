void main() {
    double num1, num2, num3, num4, num5 = 0;
    IO.println("informe os numero 1 por favor");
    num1 = Double.parseDouble(IO.readln());

    IO.println("informe os numero 2 por favor");
    num2 = Double.parseDouble(IO.readln());

    IO.println("informe os numero 3 por favor");
    num3 = Double.parseDouble(IO.readln());

    IO.println("informe os numero 4 por favor");
    num4 = Double.parseDouble(IO.readln());

    IO.println("informe os numero 5 por favor");
    num5 = Double.parseDouble(IO.readln());

    IO.println(num1+num2+num3+num4+num5);
}